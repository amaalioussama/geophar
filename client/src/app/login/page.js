import Login from "../components/login";

const Page = () => {
  return (
    <div className="flex justify-center items-center h-screen bg-white">
      <Login />
    </div>
  );
};

export default Page;
